syntax = "proto3";

option csharp_namespace = "GrpcService1";

service UserData{
	rpc GetUserData(SendUserData) returns (Reply); 
}

message SendUserData{
	string name = 1;
	string cnp = 2;
}

message Reply{
	string message = 1;
}